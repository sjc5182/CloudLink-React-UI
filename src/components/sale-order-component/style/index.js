import styled from 'styled-components'
import { font, color, grid } from '../../../globals/'

export const SalesOrderComponentStyle = styled.div`
  margin-top: ${grid.scale(2)};
  display: flex;
  flex-direction: column;
  .sales-order-main-component-wrapper{
    display:flex;
  }
`
